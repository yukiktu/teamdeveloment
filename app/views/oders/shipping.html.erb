/admin_users/orders/shipping
<% if flash[:notice].present? %>
	flash[:notice]
<% else %>

<% @oders.each do |t| %>
	<table>
		<caption>発送準備中</caption>
		<caption>
			<%= t.addressee%><br>
			<%= t.postal_code %><br>
			<%= t.adress %>
		</caption>
		<thead>
			<tr>
				<th>購入日</th>
				<th>購入者</th>
				<th>商品</th>
				<th>アーティスト</th>
				<th>購入枚数</th>
				<th>価格(税抜き)</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				<% @order_items.each do |i| %>
					<td><%= t.created_at %></td>
					<td><%= t.user.last_name t.user.first.name%></td>
					<td><%= i.name %></td>
					<td><%= i.artist_name %><td>
					<td><%= i.item_count %></td>
					<td><%= i.list_price %></td>
				<% end %>
			</tr>
		</tbody>
	</table>
<% end %>
<% end %>
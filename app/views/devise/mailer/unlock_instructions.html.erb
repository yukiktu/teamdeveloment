<p>こんにちは <%= @resource.email %>!</p>

<p>ログイン試行の失敗回数が多すぎるため、アカウントをロックしました。</p>

<p>以下のリンクをクリックして、アカウントのロックを解除できます。</p>

<p><%= link_to 'Unlock my account', unlock_url(@resource, unlock_token: @token) %></p>
